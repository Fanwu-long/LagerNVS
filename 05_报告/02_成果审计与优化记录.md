# 成果审计与本地优化记录（2026-09-09）

## 审计结论（严重度）

### 已确认非严重 / 可接受
- 三组定量指标与官方一致，500 场景分数文件完整（各 500 行 + Aggregate）
- Top-20 / sample10 / showcase / orbit 视频文件齐全，无空文件
- Bottom 场景失败模式符合 NVS 预期（大视差 / 薄结构 / 远景模糊），非数据损坏

### 已发现并完成本地修复
1. **复现配置缺失（严重）**：本地无 `re10k_2v_500.json` 与 `eval_*_500.yaml` → 已从分数 + 官方全量 JSON / 作业日志重建
2. **Re10k 256 分数命名混乱（中）**：仅以 `scores_iter_000000.txt` 存在 → 已规范为 `re10k_2view_256_500scenes.txt` 并放入 `01_scores`
3. **只展示好例（中）**：缺差例画廊 → 已从阶段一 500 场景 GT/Pred 生成 `08_bottom10_gt_pred` + 报告插图
4. **报告路径过时（轻）**：仍写 `lagernvs_data/` → 已更新为当前编号目录

### 已知局限（需算力才能再优化，本次未重跑 GPU 评测）
1. Showcase 视频码率偏低（约 170–370KB / 100 帧），观感软于 orbit_v4；若要海报级需重新导出更高码率
2. DL3DV 定量未跑（报告已诚实披露）
3. Interactive 已做功能优化（见 `07_交互式渲染演示/06_交互功能优化/`）；离线 orbit 可本地打开；实时服务需短时 GPU
4. `cdf439…` 6v 路径失败已有替代，无需再修

## 交互渲染功能优化（2026-09-09）
- Viewer：动态 WS、画质三档、自动环绕、截图、自适应 pose
- Server：运行时 JPEG 质量、`--bind`
- Offline：`offline_orbit_viewer.html`
- 平台：`HF token` 已写入 `~/.cache/huggingface/token`；启动脚本 `06_run_interactive.sh`
- **平台已同步（APPLY_OK）**：`lagernvs_interactive_opt.tgz` → sha16 `10a20ff0b12f0d2a`；`interactive_viewer.html` 含 `location.host`；`run_interactive_server.py` 含 `settings`；`06_run_interactive.sh` 已就位
- 实时演示（需短时 GPU，未自动开）：
  ```bash
  srun -p Students --qos=qos_stu_default --gres=gpu:A100:1 --cpus-per-task=4 --mem=16G -t 1:00:00 --pty bash
  cd ~/lagernvs && bash 06_run_interactive.sh --wide
  ```
  本机 SSH 隧道转发 `8765` 后打开 `http://localhost:8765`
